CREATE DATABASE IF NOT EXISTS log_viewer;
USE log_viewer;

CREATE TABLE IF NOT EXISTS logs (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) INDEX,
    data TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
